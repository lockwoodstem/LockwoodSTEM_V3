# Today’s Lesson Agenda Inline Fix

This patch places the lesson-resource buttons directly inside the agenda’s main lesson card, between the unit badge and “Today in Class.” Because the links are now generated by `agenda.js`, they remain visible in normal and full-screen views.

Replace the included files at the repository root, commit, push, and hard-refresh the agenda page. `CNAME` is not included or modified.
