# LockwoodSTEM Header Cleanup

Replace `assets/css/global-search.css` in the repository.

The update keeps the desktop header on one row, expands the global search control, compacts navigation spacing, and preserves a responsive mobile menu.
