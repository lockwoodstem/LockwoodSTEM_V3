# ADM Activity / Project / Problem Label Cleanup

Upload the contents of this folder to the root of the LockwoodSTEM GitHub repository and replace matching files.

## Changes

- Combines the redundant Lesson and Type columns on ADM Unit 1–4 landing pages.
- Displays `Activity 4.1.1`, `Project 4.1.5`, and similar identifiers as compact single-line labels.
- Uses distinct colors for Activity, Project, and Problem labels.
- Keeps the ADM lesson-page hero identifier on one line.
- Applies the cleanup consistently across all four ADM units.
- Adds a stylesheet cache-busting query to the modified ADM pages.

After GitHub Pages deploys, use a hard refresh (`Ctrl+F5`) if the previous formatting remains cached.
