# Agenda title cleanup

Extract this ZIP directly into the repository root and replace matching files.

Changes:
- Groups course, date, lesson title, and unit label into one compact title card.
- Removes the oversized floating unit pill and excessive blank space.
- Improves title hierarchy, alignment, wrapping, and mobile behavior.
- Preserves the Google Sheet agenda and due-date feeds.
